const {GoogleAuth}=require('google-auth-library');
const fs=require('fs');
const keyFile='C:/Users/wouter/Documents/spreker/despreker-c1aa07772fee.json';
async function token(){const auth=new GoogleAuth({keyFile,scopes:['https://www.googleapis.com/auth/bigquery']});const c=await auth.getClient();return (await c.getAccessToken()).token;}
async function api(url,method='GET',body){const t=await token();const r=await fetch(url,{method,headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined});const text=await r.text();let j={};try{j=text?JSON.parse(text):{}}catch{j={raw:text}};if(!r.ok) throw new Error(`${method} ${url} -> HTTP ${r.status}: ${JSON.stringify(j)}`);return j;}
async function upsertView(project,dataset,table,query){const body={tableReference:{projectId:project,datasetId:dataset,tableId:table},view:{query,useLegacySql:false}};try{await api(`https://bigquery.googleapis.com/bigquery/v2/projects/${project}/datasets/${dataset}/tables`,'POST',body);console.log(`CREATED VIEW ${dataset}.${table}`);}catch(err){const msg=String(err);if(msg.includes('Already Exists')||msg.includes('already exists')||msg.includes('duplicate')){await api(`https://bigquery.googleapis.com/bigquery/v2/projects/${project}/datasets/${dataset}/tables/${table}`,'PUT',body);console.log(`UPDATED VIEW ${dataset}.${table}`);}else throw err;}}
function ref(project,dataset,table){return '`'+project+'.'+dataset+'.'+table+'`';}
(async()=>{const raw=JSON.parse(fs.readFileSync(keyFile,'utf8'));const project=raw.project_id;const ds='intermediate_advertising'; const views={
 dim_campaign: `SELECT platform, account_id, campaign_id, ANY_VALUE(campaign_name) AS campaign_name, ANY_VALUE(channel_type) AS channel_type, ANY_VALUE(channel_subtype) AS channel_subtype, ANY_VALUE(serving_status) AS status, ANY_VALUE(bidding_strategy_type) AS bidding_strategy_type FROM ${ref(project,'staging_advertising','stg_google_ads_campaign')} GROUP BY 1,2,3 UNION ALL SELECT platform, account_id, campaign_id, ANY_VALUE(campaign_name) AS campaign_name, ANY_VALUE(campaign_type) AS channel_type, ANY_VALUE(campaign_subtype) AS channel_subtype, ANY_VALUE(campaign_status) AS status, ANY_VALUE(bidding_strategy_type) AS bidding_strategy_type FROM ${ref(project,'staging_advertising','stg_microsoft_ads_campaign')} GROUP BY 1,2,3`,
 dim_ad_group: `SELECT platform, account_id, campaign_id, ad_group_id, ANY_VALUE(ad_group_name) AS ad_group_name, ANY_VALUE(ad_group_status) AS ad_group_status, ANY_VALUE(bidding_strategy_type) AS bid_strategy_type FROM ${ref(project,'staging_advertising','stg_google_ads_ad_group')} GROUP BY 1,2,3,4 UNION ALL SELECT platform, account_id, campaign_id, ad_group_id, ANY_VALUE(ad_group_name) AS ad_group_name, ANY_VALUE(ad_group_status) AS ad_group_status, ANY_VALUE(bid_strategy_type) AS bid_strategy_type FROM ${ref(project,'staging_advertising','stg_microsoft_ads_ad_group')} GROUP BY 1,2,3,4`,
 fct_campaign_daily: `SELECT platform, account_id, campaign_id, date, device AS device_type, impressions, clicks, cost, SAFE_DIVIDE(clicks, NULLIF(impressions,0)) AS ctr, SAFE_DIVIDE(cost, NULLIF(clicks,0)) AS cpc, conversions, SAFE_DIVIDE(conversions, NULLIF(clicks,0)) AS conversion_rate, conversion_value, SAFE_DIVIDE(cost, NULLIF(conversions,0)) AS cost_per_conversion, SAFE_DIVIDE(conversion_value, NULLIF(cost,0)) AS roas, view_through_conversions, CAST(NULL AS STRING) AS network FROM ${ref(project,'staging_advertising','stg_google_ads_campaign_daily')} UNION ALL SELECT platform, account_id, campaign_id, date, device_type, impressions, clicks, cost, ctr, cpc, conversions, conversion_rate, conversion_value, cost_per_conversion, roas, view_through_conversions, network FROM ${ref(project,'staging_advertising','stg_microsoft_ads_campaign_daily')}`,
 fct_ad_group_daily: `SELECT platform, account_id, campaign_id, ad_group_id, date, device AS device_type, impressions, clicks, cost, SAFE_DIVIDE(clicks, NULLIF(impressions,0)) AS ctr, SAFE_DIVIDE(cost, NULLIF(clicks,0)) AS cpc, conversions, SAFE_DIVIDE(conversions, NULLIF(clicks,0)) AS conversion_rate, conversion_value, SAFE_DIVIDE(cost, NULLIF(conversions,0)) AS cost_per_conversion, SAFE_DIVIDE(conversion_value, NULLIF(cost,0)) AS roas, view_through_conversions, CAST(NULL AS STRING) AS network FROM ${ref(project,'staging_advertising','stg_google_ads_ad_group_daily')} UNION ALL SELECT platform, account_id, campaign_id, ad_group_id, date, device_type, impressions, clicks, cost, ctr, cpc, conversions, conversion_rate, conversion_value, cost_per_conversion, roas, view_through_conversions, network FROM ${ref(project,'staging_advertising','stg_microsoft_ads_ad_group_daily')}`
}; for(const [t,q] of Object.entries(views)){await upsertView(project,ds,t,q);} })().catch(e=>{console.error(e.stack||String(e));process.exit(1);});